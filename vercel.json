{
  "buildCommand": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client && node build-copy-public.js",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...paths]"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
